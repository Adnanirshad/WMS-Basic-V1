@model IEnumerable<WMS.Models.LvShort>

@{
    ViewBag.Title = "Index";
}
<div>
      <form class ="form-inline" role="form">
          <div class="form-group"> <h2>Short Leaves</h2> </div>
          <div class="form-group CreateBtnMargin"><p>@Html.ActionLink("Create New", "Create",null,new { @class = "btn btn-xs btn-success" })</p></div>
          <div class="form-group pull-right SearchBtnMargin">
              @using (Html.BeginForm("Index", "LvShort", FormMethod.Get))
                {<p>Find @Html.TextBox("SearchString", ViewBag.CurrentFilter as string,new { @class = "form-control" })  
                 <input class="btn btn-sm btn-success" type="submit" value="Search" /></p>}
          </div> 
      </form>  
</div>
<div class="table-responsive">
<table class="table table-bordered table-hover table-condensed">
    <tr class="table-header">
        <th>
            Date
        </th>
        <th>
            Emp No
        </th>
        <th>
            Emp Name
        </th>
        <th>
            Department
        </th>
        <th>
            Section
        </th>
        <th>
            Start Time
        </th>
        <th>
            End Time
        </th>
        <th>
            Total Time
        </th>
        <th>
            Leave Date
        </th>
        <th>
            Created By
        </th>
        <th>
            View
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
              @item.CreatedDate.Value.Date.ToString("dd-MMM-yyyy")
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Emp.EmpNo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Emp.EmpName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Emp.Section.Department.DeptName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Emp.Section.SectionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SHour)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EHour)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.THour)
        </td>
        <td>
             @item.DutyDate.Value.ToString("dd-MMM-yyyy")
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreatedBy)
        </td>
        <td>
           @* @Html.ActionLink(" ", "Edit",new { id=item.SlID }, new { @class = "glyphicon glyphicon-pencil btn btn-warning btn-xs" }) *@
            @Html.ActionLink(" ", "Details",new { id=item.SlID },new { @class = "glyphicon glyphicon-pencil btn btn-info btn-xs" })
            @*@Html.ActionLink(" ", "Delete", new { id=item.SlID },new { @class = "glyphicon glyphicon-trash btn btn-danger btn-xs" })*@
        </td>
    </tr>
}

</table>
</div>
